// = assignment
// == comparison
// === compare values and datatypes
// != inequality
// !== strict inequality

const PI = 3.14;

if (PI === "3.14") {
    console.log("This is PI");
}
else {
    console.log("This is NOT PI");
}

// NOT - because we are comparing a number to a string